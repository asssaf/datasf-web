# Project Mandates

## Environment Setup
- **Python**: For fresh sessions, ensure `python3-pip` and `python3-venv` are installed.
  - Run: `sudo apt update && sudo apt install -y python3-pip python3-venv`
